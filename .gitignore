# Python
__pycache__/
*.py[cod]
*.so
.venv/
venv/
*.egg-info/

# Local DB + terrain cache
# (on DGX the DB lives at /home/asus/sqlite/, in dev at dev/data/)
*.db
*.db-wal
*.db-shm
# repo-root data dir if anyone uses it
/data/
# local dev DB files, but keep the dir tracked
dev/data/*
!dev/data/.gitkeep
logs/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Secrets / local config
.env
.env.local
.dgx-pass
